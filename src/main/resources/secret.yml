# JWT 설정 (HS256) - Auth 서버와 동일한 secret 사용
# 운영 환경에서는 JWT_SECRET 환경변수로 override 됨 (application-prod.yml 참조)
jwt:
  # HS256 최소 32바이트(256bit) 이상
  secret: "saffron-auth-jwt-secret-key-please-change-in-production-2025"
